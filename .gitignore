# 编译产物
*.o
epubreader
EPUBReader.opk
pkg/

# 字体（用户自行放入，不入库）
font.ttf

# 工具链（构建时下载，不入库）
/opt/
# 本地手动下载的工具链缓存（154MB，绝不入库）
.tc/

# CI 回传的编译日志本地副本
build.log

# 编辑器 / 系统
.DS_Store
*.swp
*.swo

# WorkBuddy 内部数据
.workbuddy/
