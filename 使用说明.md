# Epub阅读器 v1.1.3 使用说明

> 本说明依据源码逐条整理，涵盖所有物理按键、组合键、菜单与首次部署步骤。
> 设备：GDK mini（OpenDingux / Esoteric 菜单，MIPS / uClibc / SDL 1.2）。

---

## 零、初次部署需要做的事情

1. **拷贝文件到 SD 卡**：把发布包里的 `EPUBReader.opk`（或 `EPUBReader/` 文件夹四件套：
   `EPUBReader.opk`、`default.gcw0.desktop`、`epubreader`、`epubreader_icon.png`、`launch.sh`）
   放到 SD 卡的 `/media/roms/apps/EPUBReader/` 目录（在电脑上即 **G 盘 → `apps/EPUBReader/`**）。
2. **首次启动自愈注册图标**：插回设备，从现有菜单（任意方式）启动一次阅读器。程序首次运行会
   **自动把菜单图标写入 esoteric 活目录** `$HOME/.esoteric/sections/applications/epubreader`
   （注意：必须写活目录，写出厂种子目录 `/usr/share/n/sections` 永远不会被读取）。
3. **重启设备**：退出阅读器后**重启**（Esoteric 仅在启动时读取 sections，退回菜单不会刷新），
   图标才会出现在菜单最右侧的 `apps` 分区。
4. **之后正常启动**：从菜单点击「Epub阅读器」图标即可（图标走 `opkrun` 启动，日志写在
   `/media/roms/apps/EPUBReader/EPUBReader.log`）。
5. **若图标仍未出现**：重复步骤 2–3（再启动一次 + 重启）。自检可在设备端查看 `EPUBReader.log`
   里的 `MENU REG` 段，确认自愈链接已写入活目录。
6. **自定义配置导入/导出**：位于 `/media/roms/apps/EPUBReader/epub_reader_1..9`。

> 所有配置 / 书签 / 进度均写入 `$HOME/.epubreader/`（OPK 只读挂载，故不写程序目录本身）。

---

## 一、物理按键默认映射

设备按键布局（GDK mini）：

- `A` / `B` / `X` / `Y` 为动作键；`SELECT` / `START` 为功能键；
- `L1` / `R1` 为手柄肩键（翻页）；`L2` / `R2` 为键盘映射肩键（上一页/下一页 + 组合修饰）；
- 十字方向键 = `↑` `↓` `←` `→`；音量 `+` / `-` 键用于调亮度；
- **圆 3 / 圆 4**：圆 3 为系统全局调亮度键（程序不拦截，交给系统调亮度）；圆 4 默认绑定为「显示/隐藏快捷键说明浮层」，**可在「自定义快捷键」里改绑为其它键/组合**。

| 按键 | 默认功能 |
| --- | --- |
| A | 确认 / 下一页 |
| B | 返回 / 退出（阅读页=退出阅读，其它界面=返回上一级） |
| X | 书签（添加 / 删除切换） |
| Y | 菜单 |
| SELECT | 阅读页=呼出书签光标（详见第三节）；其它界面=返回上一级。**「呼出书签光标」已作为可改绑功能（功能名：书签光标）进入「自定义快捷键」，默认 SELECT** |
| START | 息屏 / 恢复（按一次息屏，再按一次恢复） |
| L1 / R1 | 下一页 / 上一页（可在「自定义快捷键」里改绑） |
| L2 / R2 | 上一页 / 下一页（键盘映射肩键，同时作组合修饰键） |
| 十字方向键 | 上 / 下 / 左 / 右 |
| 音量 + / - | 亮度 +5% / -5% |
| **圆 4 / L1+Y** | **显示 / 隐藏本快捷键说明半透明浮层（再按一次关闭；圆 3 为系统调亮度键，改用圆 4）** |
| 圆 4 / L1+Y | 已绑定为浮层键；想换键请到「自定义快捷键」（功能名：快捷键说明）改绑 |

> 上表为**出厂默认值**。若你用过「自定义快捷键」改过绑定，请按 **L1+Y 或 圆 4** 查看当前真实按键——
> 浮层内容会**实时反映你改绑后的结果**，不必重启。

---

## 二、阅读页（正常看书）

- `↑` / `L2`：上一页（到达章首后自动跳上一章末尾）
- `↓` / `A`：下一页（到达章末后自动跳下一章开头）
- `←`：上一章（回到章首）
- `→`：下一章（回到章首）
- 书签光标键（默认 SELECT，可在「自定义快捷键」改绑）：呼出书签光标（光标落在当前页第一行，列=0）
- `B`：退出阅读，回到书架 / 文件列表
- `Y`：打开菜单
- `X`：书签：未动光标时从当前页第一行起摘录正文；若该页已有书签则再次按下为删除（切换语义）
- `START`：息屏（再按一次恢复画面）
- 底部状态栏左下角固定提示：**L1+Y 或 圆 4 查看按键说明**（任何时候都可调出快捷键说明浮层）。

---

## 三、书签光标模式（阅读页按"书签光标键"进入，默认 SELECT）

进入后屏幕在指定字处显示金色竖线光标，用于精确选择书签起点。

- `↑` / `↓`：上下换整行（光标跳到上一行 / 下一行）
- `←` / `→`：在当前行内左右逐字移动（精确定位到某个具体汉字）
- `X`：以光标位置为起点，摘录其后 23 个中文字 + `...` 添加书签
- `B` / 书签光标键：取消光标（二者等价，退出书签光标模式）
- `START`：息屏（再按一次恢复）

---

## 四、图片查看 / 缩放模式

进入方式：在含图页面按 `L1+L2` 或 `R1+R2`（详见第五节组合键）。

- `L1+L2` / `R1+R2` 再次按下：退出缩放，回到阅读页
- 缩放（需按住肩键 `L1` 或 `R1`，再按下列键）：
  - `→` / `A`：放大 10%
  - `←` / `Y`：缩小 10%
  - `↑` / `X`：放大 1%
  - `↓` / `B`：缩小 1%
- 不按肩键时：方向键平移镜头（移动查看大图的不同区域）

---

## 五、全部组合键（按住修饰键再按主键）

- `L1 + START`：强制退出 App（仅保存阅读进度，便于下次续读；**不会**往书签列表添加条目）
- `L2 + START`：强制退出 App（同上，L2 为键盘肩键）
- `R1 + START`：强制退出 App（固定别名，同上）
- `L1 + L2`：进入 / 退出图片缩放模式
- `R1 + R2`：进入 / 退出图片缩放模式（固定别名）
- 任意肩键 (`L1`/`L2`/`R1`/`R2`) + 音量 `+` / `-`：亮度按该肩键绑定的步进调整：
  - `L1 + 音量` = ±50%
  - `L2 + 音量` = ±20%
  - 其它肩键 + 音量 = ±50%（与 L1 同档）
  - 纯音量（不按肩键）= ±5%
- 说明：组合键在「自定义快捷键」设置中部分可改绑；`L1+START` / `L2+START` / `R1+R2` 为固定别名，始终生效。

---

## 六、息屏 / 待机（START 键 / 电源键）

- 在阅读页或任意界面按 `START` 一次，或按**设备电源键**一次 → 进入**待机**：屏幕熄灭、保存进度，并**把 CPU 降到最低频率**（与设备原生「待机」完全一致），耗电极低；
- 待机后除 `START` 与 `电源键` 外其它键均无效；再按 `START` 或再按 `电源键` 一次 → 立即恢复画面（两种方式均可唤醒），CPU 频率自动恢复正常；
- （退出待机只需按一次对应键，无需多次。）
- **实验特性（内核挂起，最省电）**：默认仍是上面的软件低功耗待机（背光关 + CPU 降频）。若希望进入**真正硬件挂起**（整个系统冻结、CPU 停转），可把 `$HOME/.epubreader/config.cfg` 里的 `standby_kernel` 改为 `1`。开启后若内核支持，息屏会冻结整个系统、按电源键唤醒。
  **⚠️ 警告**：若你的固件电源键**不是**有效唤醒源，设备可能需抠电池才能恢复。请先用满电设备、确认能唤醒后再长期开启。该选项默认 `0`（关闭），不影响默认行为。
- **关于电源键**：设备固件原本在电源键时向应用发送信号、默认会让应用直接退出（这正是之前「按一下电源键就退出 App」的原因）。本阅读器已接管该信号——短按电源键改为「息屏 / 恢复」，不再退出 App；长按电源键仍由系统执行关机/重启（应用层无法拦截）。若想从阅读器退回菜单，请使用 `B`（返回书架）或菜单中的「退出 App」。

---

## 七、菜单（按 Y 打开，共 8 项）

- `↑` / `↓`：上下选择菜单项
- `A` / `Y`：确认进入
- `B` / `SELECT`：返回阅读页（SELECT 在菜单即 B 语义）

菜单项：

0. **目录**：进入章节目录（见第八节）
1. **书签**：进入书签列表（见第九节）
2. **正文颜色**：文字颜色方案（5 种，↑↓ 选择，A/Y 确认）
3. **字号**：字体大小（4 档，↑↓ 选择，A/Y 确认，自动重排）
4. **亮度**：亮度微调（↑ 加 / ↓ 减，步长 5%）
5. **退出 App**：退出整个程序（保存阅读进度，不加书签）
6. **自定义快捷键**：自定义按键绑定（见第十节）
7. **返回书架**：退出阅读，回到文件列表

---

## 八、章节目录（菜单「目录」）

- `↑` / `↓`：移动选择
- `A`：父节点：展开 / 折叠；叶子节点：跳转
- `Y`：不论父子，直接跳转到该条目
- `→`：展开当前节点并进入下一级（选中首个子节点）
- `←` / `B`：返回上一级 / 返回菜单

---

## 九、书签列表（菜单「书签」 / 阅读页 X 添加）

- 阅读页按 `X` 添加书签（书签内容 = 光标/当前页起的正文摘录，最长 23 字 + `...`）；若该页已有书签，再按 `X` 为删除。
- 菜单「书签」进入列表（标题提示 `书签 [A跳转 X删除]`）：
  - `↑` / `↓`：选择书签
  - `A`：跳转到该书签所在页
  - `X`：删除该书签
  - `B` / `SELECT`：返回菜单

---

## 十、自定义快捷键（菜单「自定义快捷键」）

进入后标题显示 `自定义[←→选槽 A重绑 SEL复位] 槽x/4`，逐行列出全部可绑功能及其当前按键。

- `↑` / `↓`：选择要修改的功能（如「下一页」「菜单」…）
- `←` / `→`：切换该功能下的**绑定槽**（每功能最多 4 套，例如「下一页 = L1 / R1 都行」即两套绑定）
- `A`：进入捕获态（该功能当前槽高亮 `[ ]`），随后按下任意键或组合键即写入该槽
- `SELECT`：复位当前槽为未绑定
- `X`：删除当前槽绑定
- 冲突检测：若绑定到已被占用的组合，会提示 `冲突: <功能名>`
- `导出配置` / `导入配置`：把当前绑定写入 / 读取 `$HOME/.epubreader/epub_reader_1..9`
- `B`：返回菜单

> 改绑**立即生效**（下次按键即按新绑定执行），无需重启。改完后可随时按 **L1+Y 或 圆 4** 核对。

---

## 十一、L1+Y / 圆 4 快捷键说明浮层（重点）

- **触发方式**（两种，任选其一即可）：
  - **圆 4**：纯键盘 `sym=278`，自带圆 4 键的机器（圆 3 为系统调亮度键，程序不再占用）；
  - **L1 + Y**：按住 **L1** 肩键再按 **Y**，等同圆 4，供**无圆 4 键**的机器使用；
  - 关闭：再按一次同一组合（开启时为模态，其它输入被屏蔽）。
- **改键**：浮层键本身已作为一个可绑定功能（功能名「快捷键说明」）进入上方「自定义快捷键」菜单，默认是圆 4 / L1+Y 两套；可改绑成任意键或组合键，保存后立即生效（浮层内也会同步显示新键）。
- **字号跟随正文**：浮层文字大小 = 你在「字号」里设置的正文字号，行距随之自适应；
  当字号放大导致**内容超出屏幕**（content_h > 240）时，可用**十字方向键**或 **L1 / R1** 平移滚动查看全部。
- **内容来源**：浮层逐行读取**运行时绑定表**（`g_bind_*`），通过 `bind_label()` 实时生成
  「功能名: 当前按键」文本——因此：
  - 展示的是**你实际使用自定义快捷键功能后更新过的按键**，而非固定写死的默认值；
  - 你在「自定义快捷键」里改绑、增删槽位、导出导入后，**无需重启**，再按 L1+Y 或 圆 4 看到的即是最新结果。
- **覆盖内容**：
  - 核心动作：下一页 / 上一页 / 打开 / 返回 / 菜单 / 书签 / 书签光标 / 目录 / 息屏 / 图片缩放(进/出) / 退出 App；
  - 亮度三档（±50% / ±20% / ±5%，含你改绑的修饰键）；
  - 图片缩放方向规则（`L1/R1 + 方向` 放大/缩小），含 10%/1% 区分及不按肩键时的平移镜头；
  - SELECT 与 L1+START / L2+START（强制退出 App）；
  - 浮层自身开启/关闭说明。
- **特性**：浮层为半透明遮罩，可看到下方界面；开启时为模态（仅十字键/L1/R1 用于平移）。

---

## 十二、设置项

- **正文颜色**（菜单「正文颜色」）：5 种配色，`↑`/`↓` 选择，`A`/`Y` 确认，仅改变正文颜色（标题栏不变）。
- **字号**（菜单「字号」）：4 档，`↑`/`↓` 选择，`A`/`Y` 确认，自动重排当前页。
- **亮度**（菜单「亮度」）：`↑` 加 5% / `↓` 减 5%；也可用音量键（见第一节）。

---

## 十三、版本与文件

- 版本号：**v1.1.3**（对外固定版本号，除非用户主动要求，不再变更）。
- 发布包内容（与 G 盘 `apps/EPUBReader/` 一致）：
  `EPUBReader.opk`、`default.gcw0.desktop`、`epubreader`、`epubreader_icon.png`、`launch.sh`、
  以及本说明 `使用说明.md`。
- 日志：`/media/roms/apps/EPUBReader/EPUBReader.log`。
